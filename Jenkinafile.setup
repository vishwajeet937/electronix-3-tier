pipeline {
    agent {label 'electronix'}

    stages {
      stage("Tool Configure"){
        steps {echo "Tools Installtion pipeline"}
    }
    stage ("List Update"){
        steps {
            sh '''
            sudo apt-get update -y
            '''
        }
    }
 }
}